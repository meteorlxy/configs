# ESLint Config

## Install

```sh
npm i -D @meteorlxy/eslint-config
```

## Usage

Create `.eslintrc.cjs`:

```cjs
module.exports = {
  extends: ['@meteorlxy'],
  rules: {
    // override rules
  },
};
```
